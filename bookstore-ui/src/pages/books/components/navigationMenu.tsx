import { Flex, Button } from "@radix-ui/themes";

const NavigationMenu = () => {
  return (
    <Flex>
      <Button>Books Library</Button>
      <Button>Favorites</Button>
    </Flex>
  );
};

export default NavigationMenu;
